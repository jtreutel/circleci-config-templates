version: 2.1

jobs:
  oidc-test:
    docker:
      - image: cimg/base:2021.01
    steps:
      - checkout
      - run:
          command: | 
            echo $CIRCLE_OIDC_TOKEN > ./token.txt
            aws sts get-caller-identity

workflows:
  main:
    jobs:
      - oidc-test