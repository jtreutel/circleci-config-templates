version: 2.1
workflows:
  testing:
    jobs:
      - runner
jobs:
  runner:
    machine: true
    resource_class: jtreutel/runner-test
    steps:
      - run: echo "Hi I'm on Runners!" && sleep 60
    parallelism: 6

#more test