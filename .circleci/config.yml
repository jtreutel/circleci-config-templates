version: 2.1
workflows:
  testing:
    jobs:
      - runner
jobs:
  runner:
    machine: true
    resource_class: jtreutel/runner-test
    steps:
      - run: 
          command: echo "Hi I'm on Runners!" && sleep 1800
          no_output_timeout: 60m
    parallelism: 10